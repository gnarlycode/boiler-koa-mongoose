# This file documents possible server configuration without
# the risk of checking sensitive variables into the repository.
# Rename it to `.env` and then feel free to add sensitive values as `.env` is in the `.gitignore`.

# Server listening
HOST=0.0.0.0
PORT=8080

# Database Host
DB_HOST=_change_it_

# JWT
JWT_SECRET=_change_it_
